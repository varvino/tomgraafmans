{% extends "base.twig" %}

{% block content %}
	<div class="container">
		{% for post in posts %}
			<div class="post">
				<h1 class="post__title">{{post.title}}</h1>
				<div class="post__content">{{post.content}}</div>
			</div>
		{% endfor %}
	</div>
{% endblock %}
